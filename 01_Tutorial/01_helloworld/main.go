package main

/*
Package `main` is special -- it's not a library, it's the source for a
standalone executable.
*/

import "fmt"

// function `main()` is the entrypoint for the executable
func main() {
	fmt.Println("Hello, world")
}
